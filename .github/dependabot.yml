version: 2
updates:
  # GitHub Actions 更新由 dependabot 处理；
  # gradle / swift 依赖由 renovate.json 统一管理，避免重复 PR。
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    labels:
      - "dependencies"
