# Single source of truth for app version across Android and iOS.
# Both platforms read from this file to keep version numbers in sync.
# Update this file when bumping the version — no need to touch build.gradle
# or project.yml separately.
#
# versionName / MARKETING_VERSION: human-readable semantic version
# versionCode / CURRENT_PROJECT_VERSION: monotonic integer for store submission

versionName=4.6.0
versionCode=36
