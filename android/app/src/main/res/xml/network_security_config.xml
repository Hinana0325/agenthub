<?xml version="1.0" encoding="utf-8"?>
<!--
  网络安全配置：
  - 默认禁止明文流量（cleartextTrafficPermitted=false），所有连接强制 TLS
  - 仅对本地模型端点（10.0.2.2 模拟器 / 127.0.0.1 / 10.x / 192.168.x）
    开放明文 HTTP，因为本地回环不经过外部网络
  - 用户安装的 CA 证书仅在调试构建中信任
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- 本地模型端点（Ollama / LM Studio / llama.cpp 等）允许明文 HTTP -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">10.0.3.2</domain>
        <domain includeSubdomains="true">10.0.0.0</domain>
        <domain includeSubdomains="true">192.168.0.0</domain>
        <domain includeSubdomains="true">localhost</domain>
    </domain-config>

    <!-- 调试构建额外信任用户安装的 CA（用于代理抓包调试） -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
