package com.agenthub.app.mcp.client

/**
 * MCP 客户端 — 连接外部 MCP Server。
 *
 * MCP (Model Context Protocol) 是 Agent 生态的标准协议，
 * 允许 Agent 通过统一的接口访问外部工具和资源。
 *
 * 未来支持：
 * - GitHub MCP Server
 * - Figma MCP Server
 * - Browser MCP Server
 * - Filesystem MCP Server
 *
 * 当前为占位实现，后续版本填充。
 */
class McpClient {
    // TODO: 实现 MCP 客户端连接
}
