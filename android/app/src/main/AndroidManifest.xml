<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/AppTheme"
        android:name=".AgentControlCenterApplication"
        android:enableOnBackInvokedCallback="true"
        android:largeHeap="true"
        android:hardwareAccelerated="true">

        <!-- Sentry crash reporting (DSN injected from SENTRY_DSN env var via manifestPlaceholders) -->
        <meta-data android:name="io.sentry.dsn" android:value="${sentryDsn}" />
        <meta-data android:name="io.sentry.traces.sample-rate" android:value="1.0" />
        <meta-data android:name="io.sentry.session-replay.enabled" android:value="false" />

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true"
            android:resizeableActivity="true">
            <meta-data android:name="android.activity.allow_embedded" android:value="true" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
                P3-5: Launcher 静态快捷方式
                声明静态 shortcuts 资源，长按应用图标时展示 3 个快捷入口
                （new_chat / new_agent / settings），见 res/xml/shortcuts.xml。
            -->
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />

            <!-- Share Sheet: 接收系统分享的文本/图片 -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/*" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/*" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.SEND_MULTIPLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/*" />
            </intent-filter>

        </activity>

        <!--
            Sprint 8: Widget 快捷输入弹窗
            由 AgentControlCenterWidget.onUpdate 中 widget_input 的 PendingIntent 拉起。
            采用对话框主题、不入最近任务栈、不保留历史，营造快速弹窗体验。
        -->
        <activity
            android:name=".widget.WidgetInputActivity"
            android:exported="false"
            android:theme="@android:style/Theme.DeviceDefault.Light.Dialog.NoActionBar"
            android:excludeFromRecents="true"
            android:noHistory="true" />

        <!-- Foreground Service: 保持 Agent 后台连接 -->
        <service
            android:name=".AgentConnectionService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- App Widget -->
        <receiver
            android:name=".AgentControlCenterWidget"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/agent_control_center_widget_info" />
        </receiver>

        <!--
            Widget 语音按钮接收器：处理 Widget 上的语音按钮点击。
            Critical 2 修复：此前该类已实现（见 AgentControlCenterWidget.kt）但未在
            Manifest 注册，导致 Widget 语音按钮点击无反应。
        -->
        <receiver
            android:name=".WidgetVoiceReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.agentcontrolcenter.app.WIDGET_VOICE" />
            </intent-filter>
        </receiver>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- CI-fix: ConnectivityManager.registerNetworkCallback() 需要 ACCESS_NETWORK_STATE
         权限（ConnectionRepository.kt:123 调用），否则 Android Lint 报 MissingPermissions
         错误（3 处）。运行时也需要此权限才能监听网络状态变化。 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <!-- REQUEST_INSTALL_PACKAGES：仅用于 UpdateManager 从 GitHub Releases 下载 APK 自更新 -->
    <!-- 仅在需要自更新功能时保留；如通过应用商店分发可移除 -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <uses-feature android:name="android.hardware.microphone" android:required="false" />
</manifest>
